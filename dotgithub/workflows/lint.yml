name: lint

on:
  pull_request:
    paths:
      - "**/*.go"
      - "go.*"
  push:
    paths:
      - "**/*.go"
      - "go.*"
  workflow_dispatch:

jobs:
  lint:
    uses: spywiree/meta/.github/workflows/lint.yml@main
    with:
      generate: false
      test: false
